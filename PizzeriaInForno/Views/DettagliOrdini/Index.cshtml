@model IEnumerable<PizzeriaInForno.Models.DettagliOrdini>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ordini.Indirizzo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prodotti.NomeProd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantita)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ordini.Indirizzo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prodotti.NomeProd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantita)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDDettaglioOrd }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDDettaglioOrd }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDDettaglioOrd })
        </td>
    </tr>
}

</table>
